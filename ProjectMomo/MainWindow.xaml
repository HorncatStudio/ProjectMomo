<Window x:Class="ProjectMomo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="clr-namespace:ProjectMomo.View"
    xmlns:local="clr-namespace:ProjectMomo"
    xmlns:ViewModel="clr-namespace:ProjectMomo.ViewModel"
    Title="Project Momo" Height="350" Width="525">
  <Grid>
    <TabControl x:Name="ProjectMomoMainTabs" 
                ItemsSource="{Binding Tabs}"
                SelectedItem="{Binding SelectedTab}"
                HorizontalAlignment="Left" BorderThickness="0" Height="265" VerticalAlignment="Top" Width="507" Grid.ColumnSpan="2">
      <TabControl.Resources>
        <DataTemplate DataType="{x:Type ViewModel:HomePageViewModel}">
          <view:HomePageView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModel:PhotoGuestBookViewModel}">
          <view:PhotoGuestBookView/>
        </DataTemplate>

        <!--<Style TargetType="TabItem">
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="TabItem">
                <Grid Name="Panel">
                  <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Header"
                                                  Margin="10,2" />
                </Grid>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="Panel" Property="Background" Value="LightSkyBlue" />
                  </Trigger>
                  <Trigger Property="IsSelected" Value="False">
                    <Setter TargetName="Panel" Property="Background" Value="White" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>-->

      </TabControl.Resources>

      <TabControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Header}"/>
        </DataTemplate>
      </TabControl.ItemTemplate>

      <!--<TabControl.ContentTemplate>
        <DataTemplate DataType="x:Type ViewModel:TabViewModel">
          <view:HomePageView/>
        </DataTemplate>
        <DataTemplate DataType="x:Type vm:PhotoGuestBookViewModel">
          <view:PhotoGuestBookView/>
        </DataTemplate>
      </TabControl.ContentTemplate>-->

    
    <!--<TabItem Header="MAIN" FontSize="20">
        <Grid Background="White" Margin="0,-22,0,22"></Grid>
      </TabItem>
      <TabItem Header="GUEST BOOK" FontSize="20">
        <Grid Background="#FFE5E5E5"/>
      </TabItem>
      <TabItem Header="GIFTS" FontSize="20">
        <Grid Background="#FFE5E5E5"/>
      </TabItem>
      <TabItem Header="MANAGA" FontSize="20">
        <Grid Background="#FFE5E5E5"/>
      </TabItem>
      <TabItem Header="SETTINGS" FontSize="20">
        <Grid Background="#FFE5E5E5"/>
      </TabItem>-->
    </TabControl>
    <StatusBar Margin="0,293,0,0" Grid.ColumnSpan="2">
      <TextBlock TextWrapping="Wrap" Name="labelActiveShower" Text="TextBlock"/>
    </StatusBar>
  </Grid>

</Window>
