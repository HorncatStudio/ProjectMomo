<UserControl x:Class="ProjectMomo.View.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:ProjectMomo.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <!--<UserControl.DataContext>
    <viewModel:HomePageViewModel/>
  </UserControl.DataContext>-->
  <Grid Background="Red">
    <Button Content="{StaticResource GuestBookHeader}" HorizontalAlignment="Left" Margin="88,60,0,0" VerticalAlignment="Top" Width="75" 
            Command="{Binding ButtonCommand}"
            CommandParameter="{StaticResource GuestBookHeader}"/>

  </Grid>
</UserControl>
